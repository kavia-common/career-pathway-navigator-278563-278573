# PUBLIC_INTERFACE
# Frontend environment variables example for configuring API base URL.
# Copy this file to .env and adjust as needed.

# Base URL for the FastAPI backend (include scheme and port)
# For local dev: http://localhost:3001
# For preview: use the backend preview URL shown by the environment.
REACT_APP_API_BASE=http://localhost:3001
