# Frontend configuration (Create React App)
# NOTE: Do NOT commit real secrets. These values are for local demo only.

# Backend API base URL
# REACT_APP_API_BASE=http://localhost:3001

# Client-side demo authentication (frontend-only stub)
# Change these locally as desired; never put real credentials here.
REACT_APP_AUTH_USER=demo
REACT_APP_AUTH_PASS=demo123
